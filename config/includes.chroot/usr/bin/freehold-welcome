#!/bin/sh
# Freehold OS — first-run welcome. Opens the local welcome page once,
# then removes its own autostart entry.
rm -f "$HOME/.config/autostart/freehold-welcome.desktop"
exec firefox file:///usr/share/freehold/welcome/index.html
